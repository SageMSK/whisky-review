mixin whiskyCard(review = {})
  .whiskycard
    img(src="stuff" alt="stuff" title="stuff")
    h2.title
      a(href=`/whisky/${review.slug}`) #{review.name}
    .whiskycard__details
      p= review.description.split(' ').slice(0, 20).join(' ')