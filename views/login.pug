extends layout

block content
  form.userForm(action="/login" method="POST")
    h2= title

    label(for="email") Email
    input(type="text" name="email" required)

    label(for="password") Password
    input(type="password" name="password" required)

    input(type="submit" value="Log In")